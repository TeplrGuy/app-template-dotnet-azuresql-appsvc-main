# AUTO-GENERATED by gh-aw compile — DO NOT EDIT MANUALLY
# Source: .github/workflows/ci-failure-doctor.md
name: "CI Failure Doctor"

on:
  workflow_run:
    workflows: ["CI - Build & Test (US2)"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Workflow run ID to investigate'
        required: true
        type: string

permissions:
  actions: read
  contents: read
  issues: write
  pull-requests: read

jobs:
  ci-failure-doctor:
    if: >-
      github.event.workflow_run.conclusion == 'failure' ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install gh-aw CLI
        run: |
          curl -sL https://raw.githubusercontent.com/github/gh-aw/main/install-gh-aw.sh | bash || true

      - name: Run CI Failure Doctor
        uses: github/gh-aw/actions/setup@v0.45.0
        with:
          workflow: ci-failure-doctor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
